import { StyleSheet, Text, View, useWindowDimensions } from 'react-native'; import { Screen } from '../../src/components/Screen'; import { TopBar } from '../../src/components/TopBar'; import { NewsCard } from '../../src/components/NewsCard'; import { StateView } from '../../src/components/StateView'; import { useApiData } from '../../src/hooks/useApiData'; import { colors,type } from '../../src/theme/tokens'; import { normalizeList } from '../../src/utils/content';
export default function Newswire(){const {width}=useWindowDimensions(); const q=useApiData('/news',{limit:40},[]); const data=normalizeList(q.data); return <Screen><TopBar title="Newswire"/><View style={s.head}><Text style={s.title}>Newswire</Text><Text style={s.copy}>DEADSMILE updates, releases, and stories.</Text></View>{q.status!=='success'?<StateView loading={q.status==='loading'} error={q.error} onRetry={q.retry}/>:data.length?<View style={s.grid}>{data.map((n,i)=><View key={n.id||n.slug} style={width>760?s.half:s.full}><NewsCard item={n} large={i===0}/></View>)}</View>:<StateView empty="No news available"/>}</Screen>}
const s=StyleSheet.create({head:{paddingVertical:24},eyebrow:{fontFamily:type.bodyBold,color:colors.primary,fontSize:11,letterSpacing:1.4},title:{fontFamily:type.display,color:colors.onSurface,fontSize:54,letterSpacing:-2.5,lineHeight:56},copy:{fontFamily:type.body,color:colors.onSurfaceVariant,lineHeight:22,marginTop:12},grid:{flexDirection:'row',flexWrap:'wrap',gap:12},half:{width:'49%',flexGrow:1},full:{width:'100%'}});
