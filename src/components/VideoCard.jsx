import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Image } from 'expo-image';
import { useRouter } from 'expo-router';
import { MaterialIcon } from "./MaterialIcon";
import { colors, radius, type } from '../theme/tokens'; import { formatDate } from '../utils/content';
export function VideoCard({ item }) { const r=useRouter(); const image=item.thumbnail||item.image; return <Pressable onPress={()=>r.push(`/video/${item.id}`)} style={s.card}><View style={s.media}>{image?<Image source={{uri:image}} style={StyleSheet.absoluteFill} contentFit="cover"/>:<View style={s.placeholder}/>}<View style={s.play}><MaterialIcon name="play-arrow" size={22} color={colors.onPrimary} /></View></View><View style={s.body}><Text style={s.kicker}>WATCH · {formatDate(item.published_at)}</Text><Text style={s.title} numberOfLines={2}>{item.title}</Text></View></Pressable> }
const s=StyleSheet.create({card:{borderRadius:radius.lg,backgroundColor:colors.surface,overflow:'hidden'},media:{aspectRatio:1.78,backgroundColor:colors.surfaceContainer,justifyContent:'center',alignItems:'center'},placeholder:{...StyleSheet.absoluteFillObject,backgroundColor:colors.surfaceContainerHigh},play:{width:54,height:54,borderRadius:27,backgroundColor:colors.primary,alignItems:'center',justifyContent:'center'},body:{padding:16,gap:7},kicker:{fontFamily:type.bodyBold,color:colors.primary,fontSize:10,letterSpacing:1},title:{fontFamily:type.displayMedium,color:colors.onSurface,fontSize:22,lineHeight:23,letterSpacing:-.8}});
